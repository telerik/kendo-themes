import { Slider } from "../../slider";

export const SliderGradient = (props) => (
    <Slider
        type="gradient"
        showButtons={false}
        showTicks={false}
        {...props}>
    </Slider>
);

export default SliderGradient;

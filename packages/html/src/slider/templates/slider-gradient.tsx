import { Slider } from "../../slider/slider.spec";

export const SliderGradient = (props) => (
    <Slider
        type="gradient"
        showButtons={false}
        showTicks={false}
        {...props}>
    </Slider>
);

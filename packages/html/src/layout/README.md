```html
<!-- canonical rendering -->
<div class="
    k-{type}-layout
    {horizontal && 'k-vstack'}
    {vertical && 'k-vstack'}
    k-align-items-{alignItems}
    k-justify-content-{justifyContent}
    k-justify-items-{justifyItems}
    {wrap && 'k-flex-wrap'}
">
    {children}
</div>

```

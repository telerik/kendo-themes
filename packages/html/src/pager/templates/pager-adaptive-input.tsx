import Pager from "../pager.spec";

export const PagerAdaptiveInput = (props) => (
    <Pager
        type="input"
        adaptive={true}
        {...props}
    />
);

export default PagerAdaptiveInput;

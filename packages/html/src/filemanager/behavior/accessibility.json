{
    "ariaSpec": [
        {
            "selector": ".k-filemanager-preview",
            "attribute": "tabindex=0",
            "usage": "The element must be focusable, so that its content would be communicated to the users."
        }
    ]
}

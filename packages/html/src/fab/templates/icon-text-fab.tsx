import { FloatingActionButton } from "../fab.spec";

export const IconTextFloatingActionButton = (props) =>
    <FloatingActionButton
        icon="plus"
        text="Create"
        {...props}
    />;

export default IconTextFloatingActionButton;

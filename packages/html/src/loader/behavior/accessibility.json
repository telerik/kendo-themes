{
    "$schema": "../../../schema/accessibility.schema.json",
    "ariaSpec": [
        {
            "selector": ".k-loader-container",
            "attribute": "role",
            "value": "status",
            "usage": "Live region for loading state."
        },
        {
            "selector": ".k-loader-container",
            "attribute": "aria-live",
            "value": "polite",
            "usage": "Polite announcements of loading state changes."
        },
        {
            "selector": ".k-loader-container",
            "attribute": "aria-label",
            "usage": "Accessible name for the loader container."
        }
    ],
    "uxSpec": [],
    "kbSpec": []
}

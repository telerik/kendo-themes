# Popup Accessibility

## ARIA Attributes

The following table lists the ARIA attributes applied to the Popup component:

| Selector | Attribute | Usage |
| --- | --- | --- |
| `.k-animation-container` | `role (when appended to body)` | Container gets role="region" when appended to document body. Popup is a building block — ARIA semantics come from consuming components. |

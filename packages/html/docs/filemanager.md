# Filemanager Accessibility

## ARIA Attributes

The following table lists the ARIA attributes applied to the Filemanager component:

| Selector | Attribute | Usage |
| --- | --- | --- |
| `.k-filemanager-preview` | `tabindex=0` | The element must be focusable, so that its content would be communicated to the users. |

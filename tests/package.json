{
  "version": "0.0.0",
  "devDependencies": {
    "chai": "^4.2.0",
    "http-server": "^0.12.3",
    "jquery": "^3.5.1",
    "karma": "^5.2.3",
    "karma-chrome-launcher": "^3.1.0",
    "karma-mocha": "^2.0.1",
    "karma-mocha-reporter": "^2.2.5",
    "mocha": "^8.2.0",
    "pastshots": "^1.5.2"
  },
  "scripts": {
    "clean:tests": "node ./scripts/cleanup-test-results.js",
    "karma": "karma start",
    "karma-single": "karma start --no-auto-watch --single-run --browsers ChromeHeadless",
    "karma-travis": "karma start --no-auto-watch --single-run --browsers ChromeHeadless --reporters progress --travis",
    "karma-github": "karma start --no-auto-watch --single-run --browsers ChromeHeadless --reporters progress --gh-actions"
  }
}
